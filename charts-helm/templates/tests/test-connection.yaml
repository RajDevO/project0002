apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "charts-helm.fullname" . }}-test-connection"
  labels:
    {{- include "charts-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "charts-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
